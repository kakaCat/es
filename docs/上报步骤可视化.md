# 创建容器组上报步骤可视化

## 上报流程图

```
                创建容器组完整流程（含10次上报）
                
┌─────────────────────────────────────────────────────────┐
│  用户发起创建请求                                        │
│  POST /clusters                                          │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  Manager服务：记录元数据（⭐第一步）                     │
│  - 检查租户配额                                          │
│  - 保存TenantContainer (status=creating)                 │
│  - 保存DeploymentStatus (status=creating)                │
│  ❌ 失败则直接返回，不继续                               │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  执行 cluster.sh create                                  │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  📊 上报 #1: starting                                    │
│  - 时机：在创建任何资源之前                              │
│  - 内容：用户、服务名、命名空间、所有配置参数            │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  创建 Namespace                                          │
│  kubectl create namespace $NAMESPACE                     │
│  kubectl label namespace $NAMESPACE es-cluster=true      │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  📊 上报 #2: namespace_created                           │
│  - 时机：Namespace创建成功后                             │
│  - 内容：命名空间信息                                    │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  从GitLab拉取配置（如果提供了URL）                       │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  📊 上报 #3: gitlab_pulled                               │
│  - 时机：GitLab配置拉取完成后                            │
│  - 内容：拉取状态                                        │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  应用Kubernetes核心资源                                  │
│  kubectl apply -k k8s/overlays/dev                       │
│  - StatefulSet: elasticsearch                            │
│  - Service: elasticsearch, kibana                        │
│  - PVC: elasticsearch-data-elasticsearch-0               │
│  - Deployment: kibana                                    │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  📊 上报 #4: k8s_applied                                 │
│  - 时机：核心资源创建成功后                              │
│  - 内容：创建的资源列表                                  │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  配置资源限制                                            │
│  - 设置副本数                                            │
│  - 配置CPU和内存限制                                     │
│  - 添加索引限制注解                                      │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  📊 上报 #5: resources_configured                        │
│  - 时机：资源限制设置完成后                              │
│  - 内容：CPU、内存配置值                                 │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  配置磁盘大小                                            │
│  kubectl patch pvc elasticsearch-data-elasticsearch-0    │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  📊 上报 #6: disk_configured                             │
│  - 时机：磁盘大小配置完成后                              │
│  - 内容：磁盘大小配置                                    │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  配置GPU数量（如果GPU_COUNT > 0）                        │
│  kubectl patch sts elasticsearch                         │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  📊 上报 #7: gpu_configured                              │
│  - 时机：GPU资源配置完成后                               │
│  - 内容：GPU数量                                         │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  等待Pod就绪                                             │
│  kubectl rollout status sts/elasticsearch                │
│  kubectl rollout status deploy/kibana                    │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  📊 上报 #8: rollout_completed                           │
│  - 时机：Pod成功启动并运行后                             │
│  - 内容：就绪状态                                        │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  同步到租户容器管理                                      │
│  保存到 server/tenant_data/{user}_{service}.json         │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  📊 上报 #9: tenant_synced                               │
│  - 时机：数据同步完成后                                  │
│  - 内容：同步状态                                        │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  📊 上报 #10: completed                                  │
│  - 时机：所有步骤成功完成后                              │
│  - 内容：最终状态、完整配置信息                          │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  Manager服务：更新元数据状态                             │
│  - DeploymentStatus.Status = "created"                   │
│  - TenantContainer.Status = "created"                    │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  返回创建成功结果给用户                                  │
└─────────────────────────────────────────────────────────┘
```

## 错误处理和回滚

```
┌─────────────────────────────────────────────────────────┐
│  元数据记录失败                                          │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  ❌ 直接返回错误，不创建K8s资源                          │
└─────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────┐
│  K8s资源创建失败                                         │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  🔄 回滚操作：                                           │
│  - 删除TenantContainer记录                               │
│  - 更新DeploymentStatus.Status = "failed"                │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  返回错误信息给用户                                      │
└─────────────────────────────────────────────────────────┘
```

## 上报数据存储位置

```
每次上报都会存储到多个位置：

┌────────────────────────────────────────────────────────┐
│  1. 部署报告文件                                        │
│     server/deployment_reports/{user}_{service}_{ts}.json│
│     - 每次上报生成一个独立的JSON文件                    │
│     - 包含时间戳，便于追踪历史                          │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  2. 部署日志文件                                        │
│     /tmp/deployment.log                                 │
│     - 追加写入模式                                      │
│     - 每行一条记录，包含时间戳和详细信息                │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  3. 元数据服务                                          │
│     - DeploymentStatus: server/deployments.json         │
│     - TenantContainer: server/tenant_data/{user}_{service}.json│
└────────────────────────────────────────────────────────┘
```

## 查询方式

### 1. API查询

```bash
# 查询部署报告
curl http://localhost:8080/deployment/reports/{user}/{service}

# 查询部署状态
curl http://localhost:8080/deployments?user={user}&service_name={service}

# 查询租户容器
curl http://localhost:8080/tenant/containers/{user}/{service}
```

### 2. 文件查询

```bash
# 查看最新的部署报告
cat server/deployment_reports/{user}_{service}_$(ls -t server/deployment_reports/{user}_{service}_*.json | head -1 | xargs basename | cut -d'_' -f4 | cut -d'.' -f1).json | jq .

# 查看部署日志
tail -f /tmp/deployment.log

# 实时监控上报
tail -f /tmp/deployment.log | grep "{user}.*{service}"
```

### 3. 统计分析

```bash
# 统计各状态的上报次数
for status in starting namespace_created gitlab_pulled k8s_applied resources_configured disk_configured gpu_configured rollout_completed tenant_synced completed; do
    count=$(grep -l "\"status\": \"$status\"" server/deployment_reports/{user}_{service}_*.json 2>/dev/null | wc -l)
    echo "$status: $count"
done
```

## 上报数据格式

每次上报的JSON格式：

```json
{
  "user": "用户ID",
  "service_name": "服务名称",
  "namespace": "命名空间",
  "status": "状态标识",
  "message": "状态描述",
  "timestamp": "2024-01-01T00:00:00Z",
  "details": {
    "replicas": 1,
    "cpu_request": "500m",
    "cpu_limit": "2",
    "mem_request": "1Gi",
    "mem_limit": "2Gi",
    "disk_size": "10Gi",
    "gpu_count": 0,
    "dimension": 128,
    "vector_count": 10000
  }
}
```

## 相关文档

- **详细上报机制说明**: `/docs/部署上报机制说明.md`
- **时序图**: `/docs/时序图集合.md` - 第1节
- **测试脚本**: `/demo/test-deployment-reporting.sh`

## 代码位置

- **上报函数**: `/scripts/cluster.sh` - `report_deployment_status()` (Line 74-117)
- **元数据记录**: `/server/main.go` - `handleCreate()` 函数
- **回滚处理**: `/server/main.go` - `handleCreate()` 函数的错误处理部分
